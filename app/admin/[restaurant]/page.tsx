'use client';
import '../../qr-menu/admin/premium-v2.css';import{useEffect,useMemo,useState}from'react';import{useParams}from'next/navigation';import{BarChart3,Bell,ChevronLeft,Edit3,GripVertical,Home,MoreVertical,Plus,QrCode,Search,Settings,Star,Store,Tags,Trash2,Utensils}from'lucide-react';
type Category={id:string;name:string;isActive:boolean;sortOrder:number};type Product={id:string;categoryId:string;name:string;description:string;price:number;image:string;allergens?:string[];isActive:boolean;sortOrder:number};type Data={restaurant:{id:string;slug:string;name:string};categories:Category[];products:Product[]};type Tab='overview'|'menu'|'categories'|'qr'|'restaurant'|'feedback'|'stats';
export default function Page(){const params=useParams<{restaurant:string}>(),slug=String(params.restaurant||'').toLowerCase();const[data,setData]=useState<Data|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState(''),[saving,setSaving]=useState(false),[tab,setTab]=useState<Tab>('overview'),[query,setQuery]=useState(''),[category,setCategory]=useState('all'),[editing,setEditing]=useState<Product|null>(null),[isNew,setIsNew]=useState(false);const load=async()=>{setLoading(true);try{const r=await fetch(`/api/qr-menu/${slug}`,{cache:'no-store'});if(!r.ok)throw new Error(r.status===404?'Restoran bulunamadı':'Panel yüklenemedi');setData(await r.json());setError('')}catch(e){setError(e instanceof Error?e.message:'Panel yüklenemedi')}finally{setLoading(false)}};useEffect(()=>{if(slug)void load()},[slug]);const products=data?.products||[],categories=data?.categories||[],filtered=useMemo(()=>products.filter(p=>(category==='all'||p.categoryId===category)&&(!query||`${p.name} ${p.description}`.toLocaleLowerCase('tr-TR').includes(query.toLocaleLowerCase('tr-TR')))),[products,category,query]);const persist=async(next:Product[])=>{if(!data)return;const prev=data;setData({...data,products:next});setSaving(true);try{const r=await fetch(`/api/qr-menu/${slug}`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({products:next})});if(!r.ok)throw new Error();await load()}catch{setData(prev);alert('Değişiklik kaydedilemedi.')}finally{setSaving(false)}};if(loading)return <main className="qrAdminPremium"><div style={{padding:40}}>Panel yükleniyor...</div></main>;if(error||!data)return <main className="qrAdminPremium"><div style={{padding:40}}><h1>{error}</h1></div></main>;const nav:[Tab,string,any][]=[['overview','Genel Bakış',Home],['menu','Menüm',Utensils],['categories','Kategoriler',Tags],['qr','QR Kod',QrCode],['restaurant','Restoran Bilgileri',Store],['feedback','Geri Bildirimler',Star],['stats','İstatistikler',BarChart3]];const openNew=()=>{setEditing({id:`new-${Date.now()}`,categoryId:category==='all'?(categories[0]?.id||''):category,name:'',description:'',price:0,image:'',allergens:[],isActive:true,sortOrder:products.length+1});setIsNew(true)};return <main className="qrAdminPremium"><header className="qaTop"><button className="qaBrand" onClick={()=>setTab('overview')}><span>panel</span>takip<small>QR MENÜ</small></button><div className="qaTopRestaurant"><div className="qaRestaurantThumb">{data.restaurant.name[0]}</div><div><b>{data.restaurant.name}</b><small>{saving?'Kaydediliyor...':'Yönetim Paneli'}</small></div></div><div className="qaTopActions"><button className="qaBell"><Bell size={18}/></button><div className="qaAvatar">{data.restaurant.name.slice(0,2).toUpperCase()}</div></div></header><aside className="qaSide">{nav.map(([id,label,I])=><button key={id} className={tab===id?'active':''} onClick={()=>setTab(id)}><I size={17}/>{label}</button>)}<button><Settings size={17}/>Ayarlar</button></aside><section className="qaMain">{tab==='overview'&&<div className="qaPage"><div className="qaGreeting"><h1>{data.restaurant.name}</h1><p>Menünüz yayında. Her şey yolunda.</p></div><div className="qaMetricGrid"><Metric v={products.filter(p=>p.isActive).length} l="Yayındaki Ürün"/><Metric v={products.length} l="Toplam Ürün"/><Metric v={products.filter(p=>!p.isActive).length} l="Pasif Ürün"/><Metric v={categories.length} l="Kategori"/></div><div className="qaPromo"><div><b>Lezzet dijitalde daha yakın.</b><span>QR menünüz her zaman açık.</span></div><button onClick={()=>setTab('menu')}>Menümü Yönet</button></div></div>}{tab==='menu'&&<div className="qaPage"><div className="qaTitleRow"><div><h1>Menüm</h1><p>Ürünleri yönetin.</p></div><button className="qaAdd" onClick={openNew}><Plus size={15}/>Ürün Ekle</button></div><label className="qaSearch"><Search size={16}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Ürün ara..."/></label><div className="qaCategoryChips"><button className={category==='all'?'active':''} onClick={()=>setCategory('all')}>Tümü</button>{categories.map(c=><button key={c.id} className={category===c.id?'active':''} onClick={()=>setCategory(c.id)}>{c.name}</button>)}</div><div className="qaProductList">{filtered.map(p=><div className="qaProductRow" key={p.id} onClick={()=>{setEditing(p);setIsNew(false)}}><img src={p.image||'/makarilla_tabak.png'} alt=""/><div><b>{p.name}</b><small>{categories.find(c=>c.id===p.categoryId)?.name}</small><strong>₺{p.price}</strong></div><button className={p.isActive?'qaToggle on':'qaToggle'} onClick={e=>{e.stopPropagation();void persist(products.map(x=>x.id===p.id?{...x,isActive:!x.isActive}:x))}}><i/></button><MoreVertical size={18}/></div>)}</div></div>}{tab==='categories'&&<div className="qaPage"><h1>Kategoriler</h1><p>Menü kategorileriniz.</p><div className="qaCategoryList">{categories.map(c=><div key={c.id}><GripVertical/><img src={products.find(p=>p.categoryId===c.id)?.image||'/makarilla_tabak.png'} alt=""/><div><b>{c.name}</b><small>{products.filter(p=>p.categoryId===c.id).length} ürün</small></div><button><Edit3 size={14}/></button></div>)}</div></div>}{!['overview','menu','categories'].includes(tab)&&<div className="qaPage"><h1>{nav.find(x=>x[0]===tab)?.[1]}</h1><p>Bu bölüm sonraki aşamada etkinleştirilecek.</p></div>}</section>{editing&&<Editor p={editing} cats={categories} isNew={isNew} close={()=>{setEditing(null);setIsNew(false)}} save={async p=>{setEditing(null);setIsNew(false);await persist(isNew?[...products,p]:products.map(x=>x.id===p.id?p:x))}} del={async id=>{if(confirm('Bu ürünü silmek istiyor musunuz?')){setEditing(null);await persist(products.filter(x=>x.id!==id))}}}/>}</main>}
function Metric({v,l}:{v:number;l:string}){return <div className="qaMetric"><i>◉</i><strong>{v}</strong><span>{l}</span></div>}
function Editor({p,cats,isNew,close,save,del}:{p:Product;cats:Category[];isNew:boolean;close:()=>void;save:(p:Product)=>void;del:(id:string)=>void}){const[d,setD]=useState(p);return <div className="qaEditorBackdrop"><section className="qaEditor"><header><button onClick={close}><ChevronLeft/></button><h2>{isNew?'Ürün Ekle':'Ürün Düzenle'}</h2><button disabled={isNew} onClick={()=>del(d.id)}><Trash2/></button></header><div className="qaEditorPhoto"><img src={d.image||'/makarilla_tabak.png'} alt=""/></div><label>Ürün Adı<input value={d.name} onChange={e=>setD({...d,name:e.target.value})}/></label><label>Açıklama<textarea value={d.description} onChange={e=>setD({...d,description:e.target.value})}/></label><label>Görsel URL<input value={d.image} onChange={e=>setD({...d,image:e.target.value})}/></label><div className="qaFormGrid"><label>Kategori<select value={d.categoryId} onChange={e=>setD({...d,categoryId:e.target.value})}>{cats.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>Fiyat<div className="qaPrice"><span>₺</span><input type="number" value={d.price} onChange={e=>setD({...d,price:Number(e.target.value)})}/></div></label></div><div className="qaVisibility"><span>Menüde Göster</span><button className={d.isActive?'qaToggle on':'qaToggle'} onClick={()=>setD({...d,isActive:!d.isActive})}><i/></button></div><button className="qaSave" disabled={!d.name.trim()} onClick={()=>save({...d,name:d.name.trim()})}>Değişiklikleri Kaydet</button></section></div>}
